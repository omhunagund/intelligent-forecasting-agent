# Environment configuration template
# Real secrets belong in .env and must never be committed.

# LLM configuration will be added after the LLM provider/model is finalized.

# Database configuration will be added after the database configuration is finalized.